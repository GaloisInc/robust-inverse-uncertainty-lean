# Building the note

Run:

```bash
latexmk -pdf main.tex
```

The generated `main.pdf` is tracked so the note can be read without a local
TeX installation. Auxiliary LaTeX files are ignored.

Copyright 2026 Galois, Inc. The paper is not covered by the repository's
Apache-2.0 software license.
